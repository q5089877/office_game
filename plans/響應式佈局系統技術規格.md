# 響應式佈局系統技術規格

## 檔案結構
```
src/
├── utils/
│   ├── responsiveConfig.ts      # 響應式配置常數
│   ├── CanvasScaleManager.ts    # 畫布縮放管理器
│   ├── TouchInteraction.ts      # 觸控互動系統
│   └── DeviceDetector.ts        # 裝置檢測工具
├── components/
│   └── Layout/
│       ├── ResponsiveLayout.tsx # 響應式佈局組件
│       ├── MobileLayout.tsx     # 手機佈局
│       ├── TabletLayout.tsx     # 平板佈局
│       ├── DesktopLayout.tsx    # 桌面佈局
│       └── WideLayout.tsx       # 寬螢幕佈局
└── hooks/
    ├── useResponsiveLayout.ts   # 響應式佈局Hook
    └── useTouchInteraction.ts   # 觸控互動Hook
```

## 1. 響應式配置常數 (`src/utils/responsiveConfig.ts`)

### 斷點系統
```typescript
export const BREAKPOINTS = {
  MOBILE: 320,      // 手機最小寬度
  PHABLET: 480,     // 大手機/小平板
  TABLET: 768,      // 平板
  DESKTOP: 1024,    // 桌面
  WIDE: 1280,       // 寬螢幕
  ULTRA_WIDE: 1920, // 超寬螢幕
  MAX_WIDTH: 2560   // 最大支援寬度
};
```

### 佈局模式類型
```typescript
export type LayoutMode = 'mobile' | 'tablet' | 'desktop' | 'wide' | 'ultra-wide';
export type DeviceType = 'phone' | 'phablet' | 'tablet' | 'desktop' | 'wide-desktop';
export type Orientation = 'portrait' | 'landscape';
```

### 側邊欄配置
```typescript
export const LAYOUT_CONFIG = {
  SIDEBAR: {
    WIDTH: {
      MOBILE: '100%',      // 手機：全寬
      TABLET: '280px',     // 平板：固定寬度
      DESKTOP: '320px',    // 桌面：固定寬度
      WIDE: '360px',       // 寬螢幕：稍寬
      ULTRA_WIDE: '400px'  // 超寬螢幕：更寬
    },
    COLLAPSIBLE: {
      MOBILE: true,        // 手機：可折疊
      TABLET: true,        // 平板：可折疊
      DESKTOP: false,      // 桌面：固定顯示
      WIDE: false,         // 寬螢幕：固定顯示
      ULTRA_WIDE: false    // 超寬螢幕：固定顯示
    }
  }
};
```

### 縮放策略
```typescript
export const SCALE_STRATEGIES = {
  MOBILE: {
    MIN_SCALE: 0.25,       // 最小縮放比例
    MAX_SCALE: 0.7,        // 最大縮放比例
    PADDING_FACTOR: 0.8,   // 緩衝係數
    ASPECT_RATIO_ADJUST: 0.9
  },
  TABLET: {
    MIN_SCALE: 0.35,
    MAX_SCALE: 0.9,
    PADDING_FACTOR: 0.85,
    ASPECT_RATIO_ADJUST: 0.95
  },
  DESKTOP: {
    MIN_SCALE: 0.5,
    MAX_SCALE: 1.2,
    PADDING_FACTOR: 0.9,
    ASPECT_RATIO_ADJUST: 1.0
  }
};
```

### 響應式文字系統
```typescript
export const RESPONSIVE_TEXT = {
  BASE: {
    MOBILE: '0.875rem',    // 14px
    TABLET: '0.9375rem',   // 15px
    DESKTOP: '1rem',       // 16px
    WIDE: '1.0625rem',     // 17px
    ULTRA_WIDE: '1.125rem' // 18px
  },

  HEADING: {
    h1: { MOBILE: 2.0, TABLET: 2.25, DESKTOP: 2.5, WIDE: 2.75, ULTRA_WIDE: 3.0 },
    h2: { MOBILE: 1.75, TABLET: 1.875, DESKTOP: 2.0, WIDE: 2.125, ULTRA_WIDE: 2.25 }
  }
};
```

### 工具函數
```typescript
// 根據寬度獲取佈局模式
export function getLayoutMode(width: number): LayoutMode {
  if (width < BREAKPOINTS.TABLET) return 'mobile';
  if (width < BREAKPOINTS.DESKTOP) return 'tablet';
  if (width < BREAKPOINTS.WIDE) return 'desktop';
  if (width < BREAKPOINTS.ULTRA_WIDE) return 'wide';
  return 'ultra-wide';
}

// 獲取側邊欄寬度
export function getSidebarWidth(layoutMode: LayoutMode): string {
  return LAYOUT_CONFIG.SIDEBAR.WIDTH[layoutMode.toUpperCase() as keyof typeof LAYOUT_CONFIG.SIDEBAR.WIDTH];
}

// 獲取縮放策略
export function getScaleStrategy(layoutMode: LayoutMode) {
  return SCALE_STRATEGIES[layoutMode.toUpperCase() as keyof typeof SCALE_STRATEGIES];
}
```

## 2. 畫布縮放管理器 (`src/utils/CanvasScaleManager.ts`)

### 核心功能
```typescript
export class CanvasScaleManager {
  // 計算最佳縮放比例
  static calculateOptimalScale(
    availableWidth: number,
    availableHeight: number,
    baseWidth: number,
    baseHeight: number,
    layoutMode: LayoutMode
  ): number {
    const strategy = getScaleStrategy(layoutMode);
    const scaleX = availableWidth / baseWidth;
    const scaleY = availableHeight / baseHeight;
    const minScale = Math.min(scaleX, scaleY) * strategy.PADDING_FACTOR;

    return Math.max(strategy.MIN_SCALE, Math.min(strategy.MAX_SCALE, minScale));
  }

  // 計算畫布尺寸
  static calculateCanvasDimensions(
    scale: number,
    baseWidth: number,
    baseHeight: number
  ): { width: number; height: number } {
    return {
      width: baseWidth * scale,
      height: baseHeight * scale
    };
  }

  // 處理視窗大小變化
  static handleResize(
    callback: (scale: number, dimensions: { width: number; height: number }) => void,
    baseWidth: number,
    baseHeight: number,
    layoutMode: LayoutMode
  ): () => void {
    const handleResize = () => {
      const availableWidth = window.innerWidth;
      const availableHeight = window.innerHeight;
      const scale = this.calculateOptimalScale(
        availableWidth,
        availableHeight,
        baseWidth,
        baseHeight,
        layoutMode
      );
      const dimensions = this.calculateCanvasDimensions(scale, baseWidth, baseHeight);
      callback(scale, dimensions);
    };

    // 防抖動處理
    const debouncedHandleResize = debounce(handleResize, 150);
    window.addEventListener('resize', debouncedHandleResize);
    handleResize(); // 初始計算

    return () => window.removeEventListener('resize', debouncedHandleResize);
  }
}
```

### 防抖動工具函數
```typescript
function debounce<T extends (...args: any[]) => any>(
  func: T,
  wait: number
): (...args: Parameters<T>) => void {
  let timeout: NodeJS.Timeout;
  return (...args: Parameters<T>) => {
    clearTimeout(timeout);
    timeout = setTimeout(() => func(...args), wait);
  };
}
```

## 3. 裝置檢測工具 (`src/utils/DeviceDetector.ts`)

### 裝置資訊
```typescript
export interface DeviceInfo {
  type: DeviceType;
  layoutMode: LayoutMode;
  orientation: Orientation;
  width: number;
  height: number;
  pixelRatio: number;
  isTouchDevice: boolean;
  isMobile: boolean;
  isTablet: boolean;
  isDesktop: boolean;
}

export class DeviceDetector {
  static getDeviceInfo(): DeviceInfo {
    const width = window.innerWidth;
    const height = window.innerHeight;
    const layoutMode = getLayoutMode(width);
    const deviceType = getDeviceType(width);
    const orientation = getOrientation(width, height);

    return {
      type: deviceType,
      layoutMode,
      orientation,
      width,
      height,
      pixelRatio: window.devicePixelRatio || 1,
      isTouchDevice: 'ontouchstart' in window || navigator.maxTouchPoints > 0,
      isMobile: layoutMode === 'mobile',
      isTablet: layoutMode === 'tablet',
      isDesktop: ['desktop', 'wide', 'ultra-wide'].includes(layoutMode)
    };
  }

  // 監聽裝置變化
  static onDeviceChange(callback: (info: DeviceInfo) => void): () => void {
    const handleResize = () => {
      callback(this.getDeviceInfo());
    };

    window.addEventListener('resize', handleResize);
    window.addEventListener('orientationchange', handleResize);

    return () => {
      window.removeEventListener('resize', handleResize);
      window.removeEventListener('orientationchange', handleResize);
    };
  }
}
```

## 4. 觸控互動系統 (`src/utils/TouchInteraction.ts`)

### 觸控事件處理
```typescript
export class TouchInteraction {
  // 最小觸控目標尺寸
  static readonly MIN_TOUCH_TARGET = 44;

  // 檢查元素是否滿足最小觸控目標
  static ensureTouchTarget(element: HTMLElement): void {
    const rect = element.getBoundingClientRect();
    const width = rect.width;
    const height = rect.height;

    if (width < this.MIN_TOUCH_TARGET || height < this.MIN_TOUCH_TARGET) {
      const scale = Math.max(
        this.MIN_TOUCH_TARGET / width,
        this.MIN_TOUCH_TARGET / height
      );
      element.style.transform = `scale(${scale})`;
      element.style.transformOrigin = 'center';
    }
  }

  // 手勢識別：捏合縮放
  static setupPinchToZoom(
    element: HTMLElement,
    onScaleChange: (scale: number) => void
  ): () => void {
    let initialDistance = 0;
    let currentScale = 1;

    const handleTouchStart = (e: TouchEvent) => {
      if (e.touches.length === 2) {
        const dx = e.touches[0].clientX - e.touches[1].clientX;
        const dy = e.touches[0].clientY - e.touches[1].clientY;
        initialDistance = Math.sqrt(dx * dx + dy * dy);
      }
    };

    const handleTouchMove = (e: TouchEvent) => {
      if (e.touches.length === 2) {
        e.preventDefault();
        const dx = e.touches[0].clientX - e.touches[1].clientX;
        const dy = e.touches[0].clientY - e.touches[1].clientY;
        const currentDistance = Math.sqrt(dx * dx + dy * dy);

        if (initialDistance > 0) {
          const scale = currentDistance / initialDistance;
          currentScale = Math.max(0.5, Math.min(3, scale));
          onScaleChange(currentScale);
        }
      }
    };

    const handleTouchEnd = () => {
      initialDistance = 0;
    };

    element.addEventListener('touchstart', handleTouchStart, { passive: false });
    element.addEventListener('touchmove', handleTouchMove, { passive: false });
    element.addEventListener('touchend', handleTouchEnd);
    element.addEventListener('touchcancel', handleTouchEnd);

    return () => {
      element.removeEventListener('touchstart', handleTouchStart);
      element.removeEventListener('touchmove', handleTouchMove);
      element.removeEventListener('touchend', handleTouchEnd);
      element.removeEventListener('touchcancel', handleTouchEnd);
    };
  }

  // 滑動手勢
  static setupSwipe(
    element: HTMLElement,
    onSwipe: (direction: 'left' | 'right' | 'up' | 'down') => void,
    threshold = 50
  ): () => void {
    let startX = 0;
    let startY = 0;
    let startTime = 0;

    const handleTouchStart = (e: TouchEvent) => {
      if (e.touches.length === 1) {
        startX = e.touches[0].clientX;
        startY = e.touches[0].clientY;
        startTime = Date.now();
      }
    };

    const handleTouchEnd = (e: TouchEvent) => {
      if (e.changedTouches.length === 1) {
        const endX = e.changedTouches[0].clientX;
        const endY = e.changedTouches[0].clientY;
        const deltaX = endX - startX;
        const deltaY = endY - startY;
        const deltaTime = Date.now() - startTime;

        // 檢查是否為快速滑動
        if (deltaTime < 300) {
          if (Math.abs(deltaX) > Math.abs(deltaY)) {
            // 水平滑動
            if (Math.abs(deltaX) > threshold) {
              onSwipe(deltaX > 0 ? 'right' : 'left');
            }
          } else {
            // 垂直滑動
            if (Math.abs(deltaY) > threshold) {
              onSwipe(deltaY > 0 ? 'down' : 'up');
            }
          }
        }
      }
    };

    element.addEventListener('touchstart', handleTouchStart);
    element.addEventListener('touchend', handleTouchEnd);

    return () => {
      element.removeEventListener('touchstart', handleTouchStart);
      element.removeEventListener('touchend', handleTouchEnd);
    };
  }
}
```

## 5. 響應式佈局Hook (`src/hooks/useResponsiveLayout.ts`)

### React Hook
```typescript
import { useState, useEffect } from 'react';
import { DeviceDetector, DeviceInfo } from '../utils/DeviceDetector';
import { CanvasScaleManager } from '../utils/CanvasScaleManager';
import { BREAKPOINTS } from '../utils/responsiveConfig';

export function useResponsiveLayout(
  baseWidth: number,
  baseHeight: number
) {
  const [deviceInfo, setDeviceInfo] = useState<DeviceInfo>(() => DeviceDetector.getDeviceInfo());
  const [scale, setScale] = useState(1);
  const [canvasDimensions, setCanvasDimensions] = useState({
    width: baseWidth,
    height: baseHeight
  });

  useEffect(() => {
    // 監聽裝置變化
    const unsubscribe = DeviceDetector.onDeviceChange((info) => {
      setDeviceInfo(info);

      // 重新計算縮放
      const newScale = CanvasScaleManager.calculateOptimalScale(
        info.width,
        info.height,
        baseWidth,
        baseHeight,
        info.layoutMode
      );
      setScale(newScale);

      const dimensions = CanvasScaleManager.calculateCanvasDimensions(
        newScale,
        baseWidth,
        baseHeight
      );
      setCanvasDimensions(dimensions);
    });

    return unsubscribe;
  }, [baseWidth, baseHeight]);

  // 檢查是否為行動裝置
  const isMobile = deviceInfo.layoutMode === 'mobile';
  const isTablet = deviceInfo.layoutMode === 'tablet';
  const isDesktop = ['desktop', 'wide', 'ultra-wide'].includes(deviceInfo.layoutMode);

  // 檢查方向
  const isPortrait = deviceInfo.orientation === 'portrait';
  const isLandscape = deviceInfo.orientation === 'landscape';

  return {
    deviceInfo,
    scale,
    canvasDimensions,
    isMobile,
    isTablet,
    isDesktop,
    isPortrait,
    isLandscape,
    breakpoints: BREAKPOINTS
  };
}
```

## 6. 響應式佈局組件 (`src/components/Layout/ResponsiveLayout.tsx`)

### 主佈局組件
```typescript
import React from 'react';
import { useResponsiveLayout } from '../../hooks/useResponsiveLayout';
import { CANVAS_CONFIG } from '../../config/canvasConfig';
import MobileLayout from './MobileLayout';
import TabletLayout from './TabletLayout';
import DesktopLayout from './DesktopLayout';
import WideLayout from './WideLayout';

interface ResponsiveLayoutProps {
  children: React.ReactNode;
  sidebar: React.ReactNode;
  canvas: React.ReactNode;
  bottomArea: React.ReactNode;
}

const ResponsiveLayout: React.FC<ResponsiveLayoutProps> = ({
  children,
  sidebar,
  canvas,
  bottomArea
}) => {
  const { deviceInfo, scale, canvasDimensions } = useResponsiveLayout(
    CANVAS_CONFIG.BASE_WIDTH,
    CANVAS_CONFIG.BASE_HEIGHT
  );

  // 根據佈局模式選擇不同的佈局組件
  switch (deviceInfo.layoutMode)
